import './App.css';
import Layout from "./component/layout";

function App() {
    return (
        <Layout>
            <h1>Content</h1>
            <p>Dark mode with React + tailwind</p>
            <p>Dark mode with React + tailwind</p>
            <p>Dark mode with React + tailwind</p>
            <p>Dark mode with React + tailwind</p>
        </Layout>
    )
}

export default App;
